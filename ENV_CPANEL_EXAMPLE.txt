# Backend Environment Configuration for cPanel
# Copy this content to .env file in your cPanel public_html/api directory

# Environment
NODE_ENV=production

# Database Configuration (cPanel PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_cpanel_db_username
DB_PASSWORD=your_cpanel_db_password
DB_NAME=your_cpanel_db_name
DB_SSL=true
DB_SSL_REJECT_UNAUTHORIZED=false

# JWT Configuration
JWT_SECRET=your_strong_jwt_secret_here_minimum_32_characters_long

# Google OAuth Configuration (Production)
GOOGLE_CLIENT_ID=your_production_google_client_id
GOOGLE_CLIENT_SECRET=your_production_google_client_secret

# Admin Configuration
ADMIN_EMAIL=admin@baby2world.com
ADMIN_PASSWORD=your_secure_admin_password_here

# Server Configuration
PORT=5000
HOST=0.0.0.0
FRONTEND_URL=https://baby2world.com

# SSL Configuration (for production)
SSL_CERT_PATH=./ssl/cert.pem
SSL_KEY_PATH=./ssl/key.pem
HTTPS=true

# CORS Configuration
CORS_ORIGIN=https://baby2world.com,https://www.baby2world.com

# Session Configuration
SESSION_SECRET=your_session_secret_here_different_from_jwt

# Logging Configuration
LOG_LEVEL=info
ENABLE_DEBUG=false

# Email Configuration (if needed)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
EMAIL_FROM=noreply@baby2world.com

# Security
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
